export type ContactFormInput = {
  name: string;
  email: string;
  message: string;
};

export type ProductObject = {
  id: number;
  title: string;
  subTitle: string;
  image: string;
  description: string;
  skills: string[];
};
